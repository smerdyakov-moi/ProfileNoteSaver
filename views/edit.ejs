<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>NoteEditor</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="main w-full min-h-screen bg-zinc-900 flex flex-col items-center pt-10 text-white">
    <!-- Centered filename at top -->
    <h1 class="text-3xl font-semibold mb-2"><%= filename.replace('.txt', '') %></h1>

    <div class="form w-full max-w-2xl p-6">
      <form action="/updateFile/<%= id %>/" method="post">
        <!-- Hidden field for filename -->
        <input type="hidden" name="title" value="<%= filename.replace('.txt', '') %>">

        <!-- Textarea for editing content -->
        <textarea class="block w-full rounded-md mt-2 px-5 py-2 resize-none bg-zinc-800"
                  placeholder="Revise your thoughts" 
                  name="description"><%= data %></textarea>

        <!-- Submit button -->
        <input class="mt-4 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Redress Task">
      </form>

      <!-- Link below submit button -->
      <a class="text-blue-500 inline-block mt-4" href="/">Don't wanna change your mind?</a>
    </div>
  </div>
</body>
</html>
