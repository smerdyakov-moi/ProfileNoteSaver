<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WebApp</title>
  <link rel = "stylesheet" href= "/stylesheets/style.css">
  <script src = "https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <h1 class= "text-3xl" tracking-tighter mb-3>Update User</h1>
        <form action="/update/<%= user._id %>" method="post"> <!-- Once the Update button is clicked, it routes that with req.param of an user.id-->
            <!-- Here the value <%= %> is done so that it's easy to understand what the previous values were-->
            <input class = "px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-lg" type="text" placeholder = "Name" name="name" value="<%= user.name %>" >   
            <input class = "px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-lg" type="text" placeholder = "Email" name="email" value="<%= user.email %>">  
            <input class = "px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-lg" type="text" placeholder = "Image" name="image" value="<%= user.image %>">  
            <input class = "px-5 py-2 bg-red-500 rounded-lg ml-3" type ="submit" value ="Update!">
    </div>
</body>
</html>